export interface ServerMeta {
    forgeVersion: string
    name: string
    description: string
    icon: string
    address: string
    version: string
    mainServer: boolean
    autoConnect: boolean
}
